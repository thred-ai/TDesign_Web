import { ProviderAddressPipe } from './provider-address.pipe';

describe('ProviderAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ProviderAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
