import { EventSortPipe } from './event-sort.pipe';

describe('EventSortPipe', () => {
  it('create an instance', () => {
    const pipe = new EventSortPipe();
    expect(pipe).toBeTruthy();
  });
});
