import { LiveEarthPipePipe } from './live-earth-pipe.pipe';

describe('LiveEarthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LiveEarthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
