import { CollectionPipePipe } from './collection-pipe.pipe';

describe('CollectionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
