import { UtilityInfoPipe } from './utility-info.pipe';

describe('UtilityInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilityInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
