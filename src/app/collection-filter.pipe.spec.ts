import { CollectionFilterPipe } from './collection-filter.pipe';

describe('CollectionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
