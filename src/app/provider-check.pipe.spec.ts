import { ProviderCheckPipe } from './provider-check.pipe';

describe('ProviderCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new ProviderCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
