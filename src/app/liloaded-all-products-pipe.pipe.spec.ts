import { LiloadedAllProductsPipePipe } from './liloaded-all-products-pipe.pipe';

describe('LiloadedAllProductsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LiloadedAllProductsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
