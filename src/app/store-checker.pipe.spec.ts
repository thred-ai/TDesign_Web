import { StoreCheckerPipe } from './store-checker.pipe';

describe('StoreCheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreCheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
