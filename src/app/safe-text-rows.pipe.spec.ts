import { SafeTextRowsPipe } from './safe-text-rows.pipe';

describe('SafeTextRowsPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeTextRowsPipe();
    expect(pipe).toBeTruthy();
  });
});
