import { CurrencyIconPipe } from './currency-icon.pipe';

describe('CurrencyIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyIconPipe();
    expect(pipe).toBeTruthy();
  });
});
