import { FileDownloaderPipe } from './file-downloader.pipe';

describe('FileDownloaderPipe', () => {
  it('create an instance', () => {
    const pipe = new FileDownloaderPipe();
    expect(pipe).toBeTruthy();
  });
});
