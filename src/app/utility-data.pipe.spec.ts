import { UtilityDataPipe } from './utility-data.pipe';

describe('UtilityDataPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilityDataPipe();
    expect(pipe).toBeTruthy();
  });
});
