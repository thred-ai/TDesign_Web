import { StoreBannerPipe } from './store-banner.pipe';

describe('StoreBannerPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreBannerPipe();
    expect(pipe).toBeTruthy();
  });
});
