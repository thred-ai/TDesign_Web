import { WalletProviderPipe } from './wallet-provider.pipe';

describe('WalletProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
