import { OwnedCollectionsPipe } from './owned-collections.pipe';

describe('OwnedCollectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnedCollectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
