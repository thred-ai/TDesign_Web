import { TokenInfoPipe } from './token-info.pipe';

describe('TokenInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TokenInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
