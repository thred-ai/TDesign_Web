import { StoreThemePipe } from './store-theme.pipe';

describe('StoreThemePipe', () => {
  it('create an instance', () => {
    const pipe = new StoreThemePipe();
    expect(pipe).toBeTruthy();
  });
});
