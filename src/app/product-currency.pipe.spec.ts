import { ProductCurrencyPipe } from './product-currency.pipe';

describe('ProductCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
